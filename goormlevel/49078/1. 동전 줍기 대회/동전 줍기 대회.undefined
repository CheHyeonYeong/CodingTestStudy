# -*- coding: utf-8 -*-
# UTF-8 encoding when using korean
n = int(input())
coins = list(map(int, input().split()))
dp = [0] * (n+1)
dp[0] = coins[0]

for i in range(1, n):
	dp[i] = max(coins[i], dp[i-1] + coins[i])

print(max(dp))