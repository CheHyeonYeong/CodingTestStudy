# -*- coding: utf-8 -*-
# UTF-8 encoding when using korean
n = int(input())
idols = list(map(int, input().split()))
k = sorted(enumerate(idols), key = lambda x: x[1], reverse = True)
print(k[0][0]+1, k[1][0]+1, k[2][0]+1)