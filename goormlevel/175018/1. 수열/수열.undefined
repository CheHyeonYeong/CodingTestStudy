# -*- coding: utf-8 -*-
# UTF-8 encoding when using korean
n = int(input())

answer = [0,1]
if n < 2:
	print(answer[n-1])
else:
	for i in range(2,n):
		answer.append(answer[i-1] + answer[i-2])
	print(answer[-1]%1000000007)
